# Kubernetes Phase

Use these manifests after the Docker-first MVP is stable.

## Goals

- Replace Docker-host orchestration with Kubernetes deployments.
- Route platform traffic through Ingress.
- Automate TLS with `cert-manager`.
- Enable autoscaling for hosted customer workloads.
