[project]
name = "domain-service"
version = "0.1.0"
description = "Domains, subdomains, and SSL orchestration service"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.116.0",
  "uvicorn[standard]>=0.35.0",
]
