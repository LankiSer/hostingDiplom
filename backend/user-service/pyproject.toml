[project]
name = "user-service"
version = "0.1.0"
description = "User, team, and membership service"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.116.0",
  "uvicorn[standard]>=0.35.0",
]
