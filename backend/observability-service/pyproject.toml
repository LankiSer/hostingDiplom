[project]
name = "observability-service"
version = "0.1.0"
description = "Logs, deploy events, and platform audit service"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.116.0",
  "uvicorn[standard]>=0.35.0",
]
