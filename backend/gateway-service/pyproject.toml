[project]
name = "gateway-service"
version = "0.1.0"
description = "API gateway entrypoint for platform clients"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.116.0",
  "uvicorn[standard]>=0.35.0",
  "PyJWT>=2.9.0",
]
